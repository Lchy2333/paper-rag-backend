# RAG 项目避坑指南

> 本文档记录项目开发中踩过的坑：每条包含 现象 / 原因 / 对策 / 本项目现状。
> 末尾的「踩坑时间线」按 **日期 + 时间** 顺序记录，便于跨 session 汇总复盘。

---

## 1. PDF 解析"水土不服"（论文场景特供）

**坑点**：论文不像网页或新闻是纯文本流。直接抽文字会得到：双栏交错（左栏读到一半跳右栏）、公式乱码（数学符号变空格）、表格丢失。

**对策**：
- 优先用能识别段落顺序的解析工具（如 unstructured / Marker）
- 降级方案：洗不干净时先只提取 Abstract、Introduction、Conclusion（排版最规矩、信息密度最高）。先保证"精"，再追求"全"

**本项目现状**：✅ 已用纯 Go 的 `ledongthuc/pdf` 按页提取文字。局限：扫描件（纯图片 PDF）提取为空，需要 OCR（未接）。

**补充（已修）**：知网（CNKI）下载的论文会在 `%%EOF` 之后追加一段 XML 元数据（如 `<FileProperty>...</FileProperty>`），而 `ledongthuc/pdf` 只读文件最后 100 字节并要求以 `%%EOF` 结尾，导致报 `missing %%EOF`。已在 `parser/pdf.go` 增加 `trimTrailingJunk`：解析前从尾部找最后一个 `%%EOF`，截断掉后面的附加内容再解析。

---

## 2. "无限"上下文导致的分块忽视

**坑点**：模型支持 1M 上下文 ≠ 可以把 30 页论文全文塞进 Prompt。超长文本喂给 LLM，模型"记住"开头结尾，遗忘中间的关键实验数据。

**对策**："黄金分块"：
- Chunk Size 控制在 512–1024 Token（约 400–800 中文字）
- **Overlap 一定要设**（100–200 Token），否则"软件工程"这类词被截断成两半，向量检索彻底搜不到

**本项目现状**：✅ 已实现：`chunk_size: 800` + `chunk_overlap: 150`，并在句子边界断块。

---

## 3. 上来就搞"多路召回"和"重排序"（过度设计）

**坑点**：被 RAPTOR（树状检索）、HyDE、Rerank 模型吸引，项目初期就引入高复杂度组件。代码复杂难调试，本地 Rerank 模型可能占满 CPU/GPU、慢到卡死。

**对策**：先用最简单的"向量余弦相似度 + Top-K 召回"。先跑通"输入问题 → 准确返回包含答案的论文片段"，流程顺滑后再考虑 Rerank 精挑。

**本项目现状**：✅ 已用"向量余弦 + Top-K"（当前 `top_k: 5`）。⚠️ 注意：`top_k` 别设 1，检索是打分排序，正确片段未必第一，建议 5 起。

---

## 4. 不做"测试集"，全靠肉眼"感觉"

**坑点**：问了 5 个问题觉得不错就以为成了，换领域（计算机→医学论文）就崩。

**对策**：建立 **10 个问题的"黄金测试集"**：手动写针对上传论文的具体问题 + 期望答案片段。每次改动（换分块、换 Embedding 模型）跑一遍，计算**命中率（Hit Rate）**，用数字判断变好还是变差。

**本项目现状**：✅ 已建立（`goldentest/`）。`temp.py` 自动生成 5 篇语料 PDF + 50 条事实库 `facts.json`（F01-F30 原子 + D01-D20 干扰，含论文/章节/行号锚点）；人工补充问题集 `golden_queries.json`；`go run ./goldentest/eval` 跑一致性校验，`tests/unit/parser_realpdf_test.go` 用真实 PDF 做解析回归。

设计要点（本次构建沉淀）：
- **锚定"语义"而非"物理 ID"**：标注挂在事实 ID 上，而不是数据库 chunk 的 UUID/idx——重新入库、改分块、换 embedding 都不会让黄金集作废
- **期望答案单一数据源**：答案文本/位置只在 `facts.json` 存一份，问题集通过 `target_fact_ids` 引用，避免两份数据不一致
- **无金标文件时测试自动跳过**：goldentest 未提交时 `go test` 照常通过

---

## 5. 忽略"拒答"能力（捏造幻觉）

**坑点**：知识库没存的信息（如解析漏掉的作者），LLM 为了讨好用户编造"张三"。

**对策**：System Prompt 强制加一句："如果根据提供的【参考资料】无法找到答案，请直接回答'根据现有资料无法确定'，严禁胡编乱造。"

**本项目现状**：✅ 已实现。`ask.go` 的 system prompt 包含"只使用资料片段作答、不要编造、资料不足要明说"，并有 `no_context_reply` 兜底（检索不到时直接返回提示）。

---

## 6. 代码耦合过深，没法拆解测试

**坑点**：读取PDF → 分块 → Embedding → 存向量库 → 检索 → 生成全写在一个 500 行 main.go 里，调个分块参数都得从头编译。

**对策**：严格分层（Ingestion / Indexing / Retrieval / Generation），每层用接口连接，换 Embedding 只改一层。

**本项目现状**：✅ 已实现。`parser / chunker / ai / store / service / httpapi` 分层清晰，`store.Store` 接口支持换实现。

---

## 7. 公式 / 符号检索失效（论文场景实测）

**现象**：原文写的是 `Ab（小）=Cd+Ef（小）`（"小"是下标），提问"Ab 如何计算"答不了；必须问 `Ab（小）` 才能检索到。

**原因**：
- PDF 提取公式时，下标/上标常被提取成普通字符或特殊形式，和提问写法不一致
- embedding 把 `Ab` 和 `Ab（小）` 当作**不同的 token**，语义向量差异大 → 检索不到
- 公式、符号、下标本质是 embedding 的弱项（它擅长"主题语义"，不擅长"符号精确匹配"）

**对策**：
- 提问时尽量用**与原文完全一致的写法**（含下标/符号形式）
- 对公式区域做**结构化提取**（LaTeX / MathML），或把公式的文本形式单独记录、向量化
- 引入**关键词/全文检索**（BM25）兜底公式、编号、专有名词这类"字面匹配"场景（混合检索）
- 公式常伴随章节上下文，可结合"标题感知切片"提升命中

**本项目现状**：🟢 已实现（2026-09 完成公式识别 + 混合检索）。
- **公式识别**：字体指纹检测独立行公式（Cambria Math / MT Extra / Euclid / Symbol / Times），PyMuPDF 渲染裁剪公式截图 → qwen2.5vl 视觉模型 OCR 还原 LaTeX，公式整块成 chunk 入库（content 存 LaTeX）。
- **混合检索（BM25 风格）**：`Search` 三路 RRF 融合——向量（pgvector HNSW）+ **tsvector 全文**（gojieba 中文分词，`ts_rank` 类 BM25 排序）+ pg_trgm 字面（公式/符号/编号兜底）。`Ab（小）` 这类字面精确场景现在能靠全文路命中。
- **中文分词**：`internal/segment` 用 gojieba（纯 Go，避免 zhparser/SCWS 在 Windows 编译的坑），分词结果存 `chunks.tsv`（`to_tsvector('simple')`）。**旧 chunks 的 tsv 为 NULL，改动后要重新上传文档**。
- **归属过滤（已修复）**：三条路都遵守 `user_id` / `document_id`（`store.buildFilterClause` 统一生成）；曾踩坑：全文路/字面路一度漏拼过滤条件，会泄漏其他用户/文档的 chunk。
- **已知局限**：方正书版论文（ZJSJ 字体）公式与正文共用字体集，纯字体指纹分不开，暂不处理（Roadmap P1）；OCR 质量受 7B 视觉模型限制（复杂公式偶发结构错）。

---

## 8. 全库检索在多文档场景下会混淆冲突（待实现能力）

**现象**：一次插入两篇结论相反的文档（例如都讲苹果颜色、但结果不同），提问时**无法指定文档**，检索会把两篇的片段混在一起，回答可能混淆甚至自相矛盾。

**原因**：当前 `/ask` 是**全库向量检索**，不限定文档范围。两篇相似主题的片段同时进入 topK，LLM 分不清应遵循哪篇的结论。

**对策（建议能力）**：
- `/ask` 增加 `document_ids` 参数，支持**指定一个或多个文档**范围内回答
- 检索 SQL 加过滤：`WHERE c.document_id = ANY($ids)`
- 前端/客户端可先选文档再提问；不传时保持全库检索
- 多文档回答时，可在 prompt 中要求标注"依据哪篇文档"，并依靠 citations 溯源

**本项目现状**：✅ 已实现（`/ask` 支持 `document_ids` 限定检索范围，见 store.go 的 `Search` 变参实现）。不传则保持全库检索；传了则在指定文档内取 top_k。

---

## 9. ledongthuc/pdf 的 T* 换行 bug（正文混入假字符）

**现象**：入库后 chunk 正文**每行结尾多一个"页"**，段落折行处混入"端/磺/境/稀/书"等字，词语被拆坏（"木质素磺酸盐"→"木质素磺磺酸盐"、"256GB"→"25页6GB"、"如何"→"如页何"）；页脚"合成科研语料集 · 第 1 页"还被塞进 chunk 开头。**5 篇 PDF 全中**，且每篇混入的"特征字"不一样。

**原因**（三层叠加）：
1. `GetPlainText` 处理 PDF 换行运算符 `T*` 时调用 `showEncodedText("\n")`，把换行字节 `0x0A` 交给**当前字体**的 ToUnicode 解码（`page.go:571`），而不是写真实换行符
2. reportlab 嵌入的 SimHei 子集字体恰好把 `0x0A` 映射成真实汉字——常规子集是"页"，粗体子集是"端/磺/境/稀/书"（每篇不同，取决于该 PDF 粗体子集里第 11 个注册字形是谁）→ 每次换行/折行都解出一个假字
3. reportlab 的页脚在**每页开头**绘制（onPage 回调），`GetPlainText` 按内容流顺序输出 → 页脚跑到了正文最前

**排查方法**：用 pdfplumber（Python）独立提取作为"真实内容"参照，与库输出 diff；或直接调库的 `Content()`（带坐标的逐字文本）对比 `GetPlainText`，立刻能定位假字来自 `T*` 处理。

**对策**：
- 首选：改用 `Content()` + 按 (Y↓, X↑) 排序重建文本（本项目 `parser/pdf.go` 的 `pageText`），彻底绕开 `T*` bug，且顺带把页脚排回页尾（真实阅读顺序）
- 备选：一行 patch（`T*` 分支 `showEncodedText("\n")` 改 `showText("\n")`），但要 fork 本地维护
- 验收：`tests/unit/parser_realpdf_test.go` + `goldentest/eval` 用 50 条事实逐字匹配把关（修复前 26/50 → 修复后 50/50）

**本项目现状**：✅ 已修复（`parser/pdf.go` 改用 `Content()` 重建文本，新增真实 PDF 回归测试）。

---

## 10. PDF 提取为空的第二个原因——Form XObject（嵌套内容流），曾误判为扫描件

**现象**：`钢管约束钢筋混凝土柱受剪承载力计算_甘丹.pdf` 上传报"PDF 未能提取到文本内容"，但 SumatraPDF 能选中文字（说明**有**文字层）。排查时曾误判为扫描件。

**原因**：
- 页面内容流只有 `q /Fm0 Do Q Q`（用 `Do` 算子"引用子表单"），真正的 `Tj/TJ` 写字指令在 **Form XObject（嵌套内容流）** 里
- `ledongthuc/pdf` 只处理页面顶层内容流、**不递归进 Form XObject**（源码无 XObject/`Do` 处理逻辑），所以提取到 0 字符
- SumatraPDF 底层 MuPDF/poppler 会递归，所以能选中文字

**排查**：用 pdfcpu 提取页面原始内容，看到 `q /Fm0 Do Q Q` 即确认是嵌套表单结构（文件本身合法，pdfcpu 能正常消化）。

**对策**：
- 换文字版 PDF（最省事）
- 或让 parser 支持 Form XObject 递归 + ToUnicode 映射（大工程，相当于写迷你文本提取器）
- 曾尝试换 pdfcpu：其 v0.15 `extract` **无 text 模式**（只有 image/font/content/page/meta），不能用于提取文字，方案作废

**本项目现状**：❌ 未处理（该 PDF 已弃用；长期可考虑换支持 Form 的解析库）。教训：**能选中文字 ≠ 解析器提取得出来**，排查"提取为空"别急着下"扫描件"结论。

---

## 11. 文件级 SHA 去重拦不住"同内容重转的 PDF"

**现象**：做上传去重时对原始文件字节算 SHA-256 存 `documents.content_hash`。但同一个 Word 转两次 PDF（内容没改），**文件字节哈希不一致**——因为 PDF 里有 `CreationDate/ModDate` 等时间戳元数据，每次生成都不同。

**原因**：文件哈希 = 字节指纹，比的是"字节完全相同"；而"内容相同" ≠ "字节相同"（元数据会变）。改一个字再转则哈希必然不同（内容流字节变了）。

**对策**：若要拦"内容相同的文档"，应改为**解析后文本哈希**（对提取的正文算 SHA，绕开元数据干扰）。文件级 SHA 只拦"字节完全相同"的重复（复制重传同一份 PDF）。

**本项目现状**：⚠️ 已实现文件级 SHA 去重（拦复制重传，可用）；"同内容重转 PDF"拦不住，已知局限。

---

## 12. reportlab 生成中文 PDF 的三连坑（生成失败 / 乱码 / 空字节入库失败）

**现象**：用 reportlab 生成合成语料 PDF 连踩三个坑：
1. **输出路径 `/mnt/data/...`**（Linux）在 Windows 上不存在 → `doc.build` 报 `FileNotFoundError`，PDF 根本没生成
2. 用 `STSong-Light` CID 字体 → 我们的解析器解不了字形映射，提取中文全乱码（`�` 一堆）
3. 表格里的上标 `²`（`g/m²/day`）被解析器映射成**空字节 `\x00`** → PostgreSQL 报 `无效的 "UTF8" 编码字节顺序: 0x00`，入库失败

**原因**：
1. 脚本从 Linux 环境迁到 Windows，路径没改
2. `UnicodeCIDFont` 不内嵌字体、依赖系统 CMap，`ledongthuc/pdf` 对它支持差
3. 特殊 Unicode 字符（上标/符号）在 PDF 字形映射里是隐患，个别会被映射成空字节

**对策**：
1. 路径改为 `os.path.join(os.path.dirname(os.path.abspath(__file__)), "testdata")` 并 `os.makedirs(..., exist_ok=True)`
2. 改用**嵌入的 TrueType 中文字体**：`pdfmetrics.registerFont(TTFont("SimHei", r"C:\Windows\Fonts\simhei.ttf"))`
3. 源头把 `²` 换成 `^2`；同时在 `parser/pdf.go` 的 `normalizeText` 剥离 `\x00` + `strings.ToValidUTF8(s, "")`（防御整类问题）

**本项目现状**：✅ 已修复（parser 兜底 + temp.py 改路径/字体/符号）。教训：**PDF 提取链路里特殊 Unicode 字符（上标、符号）是隐患**，parser 已做兜底。

---

## 踩坑时间线（跨 session 汇总用）

> 按**时间顺序**记录每次 session 踩过的坑，便于跨 session 汇总复盘。
> **每条记录都带完整"日期 + 时间"**（取自会话中的文件时间戳与命令输出，为近似值"约"），
> 这样无论怎么跨 session 混排，都能按时间排序。新 session 在下方追加新日期小节即可。
> 详细"坑点 / 对策 / 现状"见对应编号条目；无编号的为一次性/环境性小坑。
> **新 session 按日期插入对应时间位置**（保持整体从早到晚的时间顺序）。

### 2026-08-15（框架搭建 + 本地数据库 + 密码重置）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-08-15 约14:00 | pgvector 编译 `postgres.h` 找不到 | PGROOT 设成 `C:\Program Files\PostgreSQL\18`，实际装在 `D:\PostgreSQL`，编译器找不到头文件；改 `set "PGROOT=D:\PostgreSQL"` | — |
| 2026-08-15 约15:00 | pg_hba.conf 加载失败 "could not load" | PowerShell `Set-Content` 写入 UTF-8 BOM，PostgreSQL 解析不了 BOM；用无 BOM 的 `[System.IO.File]::WriteAllText(..., UTF8Encoding($false))` 重写 | — |
| 2026-08-15 约15:30 | postgres 密码认证失败 | 忘记安装时设的 postgres 密码；临时把 pg_hba.conf 改 `trust` 免密 → `ALTER USER` 重置新密码 → 改回 `scram-sha-256`（安全恢复） | — |

### 2026-08-16（PostgresStore 接入真实数据库）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-08-16 20:37 | DSN URL 格式密码特殊字符 | 连接串用 `postgres://user:pass@host`，密码含特殊字符 → `invalid userinfo`；改用键值对 `host=... password=...`（pgx 支持，密码特殊字符安全） | — |
| 2026-08-16 20:45 | NULL 字段 scan 报错 | 手工插入的行 `error`/`title` 为 NULL，Go 的 `string` 不能直接 scan NULL → `cannot scan NULL into *string`；SQL 加 `COALESCE(title,'') / COALESCE(error,'')` 兜底，并在集成测试加 NULL 用例 | — |

### 2026-08-17（完整链路跑通 + Ollama 接入 + 检索优化）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-08-17 15:36 | 外键约束顺序错误 | ingest 先 `AddChunks` 后 `SaveDocument`，chunks 子表先于父表插入 → `chunks_document_id_fkey` 违约；改为"先存文档 → 再存分块 → 最后更新状态" | — |
| 2026-08-17 15:46 | top_k=1 检索失败 | 正确片段排不进第 1（中文弱 embedding + 措辞差异），top_k=1 梭哈第 1 名必翻车；改 top_k=5，检索负责召回、LLM 负责精读 | #3 |
| 2026-08-17 约16:00 | nomic-embed-text 中文语义弱 | "80%"与"百分之多少"措辞变体匹配不上；升级 bge-m3（多语言，1024 维） | #3 |
| 2026-08-17 约16:30 | 换 embedding 模型维度连锁 | 768→1024：配置 `dimension` + 数据库列 `VECTOR(n)` + 测试向量**三处必须同步**，且旧向量坐标系不同必须**清空重传** | — |
| 2026-08-17 约17:00 | qwen3 chat 返回 content 为空 | 两个原因叠加：① PowerShell 控制台 GBK 编码把中文显示成空/乱码（数据其实没坏）；② qwen3 思考模式吃满 max_tokens（`finish_reason=length` 但 content 空）；排查要看原始 JSON | — |
| 2026-08-17 约17:20 | Postman 找不到 form-data | 新版 Postman 改名 "multipart form"；另注意别用 binary（裸文件不带 `file` 字段名） | — |
| 2026-08-17 约18:00 | 公式/下标检索失效 | 原文 `Ab（小）=Cd+Ef（小）`，问"Ab 如何计算"答不了，必须问 `Ab（小）`；embedding 对符号/下标/公式是弱项，需贴近原文提问 | #7 |
| 2026-08-17 约18:10 | 全库检索多文档冲突 | 两篇结论相反的文档无法指定范围回答（当前 `/ask` 全库检索）；建议 `document_ids` 参数限定范围 | #8 |

### 2026-08-30（本次：PDF 解析多坑 + 去重 / 环境 / 报告生成坑）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-08-30 约15:30 | 知网 PDF `missing %%EOF` | `%%EOF` 后被追加 XML 元数据（`<FileProperty>`），库要求文件末尾正好是 `%%EOF`；parser 加 `trimTrailingJunk` 截断 | #1 补充 |
| 2026-08-30 约16:00 | PDF 提取为空 → 误判扫描件 | 页面只有 `q /Fm0 Do Q Q`，文字藏在 Form XObject（嵌套内容流）；曾误判为扫描件，实为解析器不递归 | #10 |
| 2026-08-30 约16:30 | pdfcpu 无文本提取 | 想换 pdfcpu 解决，但 v0.15 `extract` 无 text 模式（只有 image/font/content/page/meta），方案作废 | #10 |
| 2026-08-30 约17:00 | 文件级 SHA 去重局限 | 同内容重转 PDF 文件字节哈希不同（时间戳元数据），文件级 SHA 拦不住"内容相同" | #11 |
| 2026-08-30 约20:50 | 测试服务端口冲突 | `config.test.yaml` 端口和生产同为 8080，两个 server 同时跑冲突；测试端口改 8081 | — |
| 2026-08-30 约21:00 | reportlab 输出路径 `/mnt/data` | Linux 路径在 Windows 报 `FileNotFoundError`；改为脚本目录下 testdata 并自动建目录 | #12 |
| 2026-08-30 约21:05 | reportlab 中文乱码 | `STSong-Light` CID 字体解析器解不了 → 改用嵌入 SimHei TTF | #12 |
| 2026-08-30 约21:10 | PDF 提取 `\x00` 空字节 | 上标 `²` 被映射成 `\x00`，PostgreSQL 拒绝入库；parser 剥离 `\x00` + ToValidUTF8 | #12 |
| 2026-08-30 约21:30 | 事实→chunk 匹配失败（22/50） | 合成 PDF 提取不忠实（"连续观测到"→"连续端观测到"），实为 ledongthuc/pdf 的 T* bug，下个 session 定位修复 | #9 |

### 2026-08-31（本次：PDF 假字符排查 + 黄金测试集搭建 + 文档更新）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-08-31 约20:45 | 数据库密码未设置 | 跑测试库查询时 `TEST_DATABASE_PASSWORD` 未设；`.idea` 里的密码是 master_key 加密读不出，只能问本人要密码 | — |
| 2026-08-31 约21:00 | **PDF 解析假字符（主坑）** | 5 篇 testdata 全中：每行尾"页"、折行处混"端/磺/境/稀/书"、页脚进正文；定位为 `GetPlainText` 的 T* 把 0x0A 交给字体解码 | #9 |
| 2026-08-31 约21:35 | Content() 懒读取需保持文件打开 | 临时工具过早 `fh.Close()`，触发解析 panic "file already closed"；解析完再 Close | — |
| 2026-08-31 约21:40 | 修复验证通过 | `go test ./tests/unit` 全过；50 条事实逐字匹配 26/50 → 50/50；新增真实 PDF 回归测试 | #9 |
| 2026-08-31 约22:30 | temp.py 意外丢失 | 清理临时文件时发现生成器脚本从磁盘消失；靠会话记录重建，并在临时目录验证可重新生成 50/50 事实语料 | — |

### 2026-09-05（黄金测试集：v2 检索评测 + v3 问答评测 + 干扰项重设计）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-09-05 约20:00 | v2 检索评测 Recall@K | 实现 `-retrieval`：一次检索按不同截断统计 `Recall@1/3/5/10 + MRR`（不是跑 4 次检索）；gold chunk 用内容锚定，事实被截断时退回部分重叠匹配 | — |
| 2026-09-05 约20:30 | v3 问答评测 LLM 裁判 | 实现 `-answer`：走真实 `/ask` 链路 + LLM 裁判判分（原子 `correct/partial/wrong`，干扰 `ok/hallucinated`）；据 citations 反推检索命中，输出端到端诊断矩阵（定位检索还是生成问题） | — |
| 2026-09-05 约20:50 | **干扰项设计缺陷（设计修正）** | 原"直接问干扰值"自相矛盾——正文里真有"90毫米"，答 90 毫米反而是正确答案，判幻觉说不通；改为"干扰项**不在语料**的假说法"（方案 A），专测编造；v1 校验干扰项必须缺席、v2 跳过干扰项、v3 判是否编造 | — |
| 2026-09-05 约21:00 | **提取式 vs 归纳式事实（设计边界）** | v1/v2 基于"事实=原文原句"设计，只支持提取式；归纳式总结没有原句可锚定，需**多个证据句 + 覆盖率指标**（命中 X/N），未来扩展 `type: "summary"`，单靠 Recall@K 会虚高 | — |
| 2026-09-05 约21:10 | 原子事实前提只能进问题 | PDF 固定不重新生成（temp.py 已删），事实原文必须与 PDF 逐字一致（v1/v2 匹配用），故"前提/语境"只能加进**问题**里而非事实文本；干扰项数值逐一验证不在语料 | — |
| 2026-09-05 约21:15 | 评测报告改 JSON | `reports/` 从 .txt 改为 `.json`（结构化 `ingest/v1/v2/v3` 四段 + 逐条问题结果），控制台只打关键指标摘要，便于查看 | — |

### 2026-09-12（公式识别 v1：字体指纹检测 → 视觉模型还原 LaTeX）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-09-12 约18:00 | 公式测试 PDF 提取乱序 | MathType/Word 自带公式转 PDF 后，文本层被压平成乱序符号（分式变两行、上下标错位、√ 变 `^...0.5`、∑/∫ 上下限拆行）；手写公式（图片）零提取。结论：**不能靠纯文本提取做公式能力**，需结构化 LaTeX | #7 |
| 2026-09-12 约18:20 | `FormulaOCR` prompt 含反引号报语法错 | Go 原始字符串字面量 `` ` `` 里写了 ``` ``` ``` 代码块标记，反引号终止了字符串；改用文字描述"三个反引号" | — |
| 2026-09-12 约18:40 | 7B 视觉模型幻觉尾巴 | 识别 `∫0→1 a=∑3 b+c` 末尾多输出 `\leftarrow`（左箭头幻觉）和 `$$...$$` 包裹；`ai.CleanLatex` 剥定界符/代码块围栏/结尾箭头命令 | — |
| 2026-09-12 约19:00 | 公式测试图放哪 | 建 `goldentest/formula/` 存放公式测试图（5 公式 × MT/Word 两种 = 10 张 PNG），识别结果 10/10 全对 | — |
| 2026-09-12 约19:30 | 公式检测内联误判 | 内联公式 `x>0` 附近中文"当/时"距公式边界 ~10pt，初始阈值 4pt 漏判；调大 `formulaInlineGap=12`（≈1×字号），独立公式行尾编号（距公式数百 pt）不受影响 | — |
| 2026-09-12 约20:00 | 公式检测跨包测试字段未导出 | `DetectFormulas` 返回的 `formulaRegion` 是私有类型，`tests/unit` 黑盒测试无法访问 `.text`；导出为 `FormulaRegion`（`Text`/`Idx`/`Top` 等） | — |
| 2026-09-12 约21:00 | **方案 C（文本还原）全乱码（设计推翻）** | 5 公式 PDF 摄入后 chunk 里公式全是 `\�frc{a�\�qsr{...}` 乱码；原因是 PDF 文本层对公式是"乱序残缺字符+`�`(U+FFFD)替换符"，信息从根上不够，视觉模型看残缺文本还原必乱；**决定改走截图路线**（渲染→裁剪→OCR） | #7 |
| 2026-09-12 约21:20 | mutool 下载迷路 | 官网 MuPDF 核心包是 C 源码（`-source`，要 VS 编译才得 exe）；官网发布页 JS 驱动猜 URL 全 404；改选 **PyMuPDF**（pip install pymupdf，内核同 MuPDF，免编译） | — |
| 2026-09-12 约21:40 | 公式检测拆碎 | 同一公式分式分子/分母、积分上下限间距达 1.5×字号，固定 mergeGap=8pt 拆散公式；改**按字号自适应**（max(8, 1.5×中位字号)） | — |
| 2026-09-12 约21:50 | 老式公式漏检 | `a=b+c` 用 TimesNewRomanPS(正体)+SymbolMT，`IsMathFont` 只认斜体漏了正体 → 公式拆没；放宽匹配 `timesnewromanps` 全系列 | — |
| 2026-09-12 约22:00 | 内联过滤误杀独立公式 | DengXian 正文的**空格/换行字形**落在公式 x 扩展范围内被判"内联"→ 独立公式被丢弃；`isInlineFormula` 跳过空白字形（`strings.TrimSpace(t.S)==""` 忽略） | — |

### 2026-09-13（混合检索：向量 + tsvector 全文 + pg_trgm 字面，三路 RRF）

| 时间 | 坑 | 简述 | 详见 |
|---|---|---|---|
| 2026-09-13 约10:30 | 甘丹论文公式全失败 | 39 chunk 无一有效 LaTeX；定位为**方正书版 ZJSJ 字体**（知网/国内期刊），公式与正文共用字体集（ga 既排正文英文又排公式数字/cot），纯字体指纹分不开 | #7 |
| 2026-09-13 约10:40 | 方正书版方案取舍 | 试 ZJSJgb/gc + `isRealFormula` 过滤：公式被拆成残缺碎片（缺数字和 cot）→ 截图残缺 → qwen2.5vl 幻觉成教科书公式（`∫sinx/x dx=π/2` 重复 6 次）；结论**暂不处理**，需改"行特征检测"（Roadmap P1） | #7 |
| 2026-09-13 约10:50 | kimi-k3 当 OCR 模型幻觉 | config.test.yaml 误配 `doubao-seedream-5.0-lite`/kimi-k3（对话/生图模型）做公式 OCR，对残缺小图疯狂幻觉；公式 OCR 应配**视觉模型**（qwen2.5vl） | — |
| 2026-09-13 约11:00 | HNSW + 过滤候选不足 | 两阶段检索：粗筛放宽 LIMIT + `hnsw.iterative_scan=relaxed_order`，应用层精筛，不足用 document_id B-tree 补漏 | — |
| 2026-09-13 约11:10 | zhparser/SCWS Windows 编译难 | 想装 zhparser 做 SQL 层中文分词，但它依赖 SCWS（Unix 工程，Windows 无官方支持），要 MSYS2/MinGW 移植；**改走 Go 层分词（gojieba，纯 Go）**，分词结果存 `chunks.tsv`（`to_tsvector('simple')`） | — |
| 2026-09-13 约11:20 | git 全局代理 127.0.0.1:7890 失效 | clone zhparser 失败：git 配了全局代理但代理没在跑；`git -c http.proxy="" -c https.proxy="" clone ...` 绕开 | — |
| 2026-09-13 约11:30 | tsvector 检索对旧数据无效 | 已入库 chunks 的 `tsv` 列全 NULL（旧代码没写）；`AddChunks` 现在写 tsv，**改动后要重新上传文档** | — |
| 2026-09-13 约11:40 | 临时工具 newID 时间戳冲突 | reingest 用纳秒时间戳做 chunk ID，与已有 ID 撞 `chunks_pkey`；改 `crypto/rand` 随机 ID | — |
| 2026-09-13 约12:00 | **混合检索全文/字面路漏过滤（主坑）** | 混合检索三路 RRF 里只有向量路做了 `user_id`/`document_id` 过滤，全文路（tsvector）和字面路（pg_trgm）的 SQL 没拼过滤条件 → 会泄漏其他用户/文档的 chunk；新增 `store.buildFilterClause` 统一生成过滤，两条路都拼上，集成测试加"他人用户/其他文档不泄漏"用例 | #7 |

---
